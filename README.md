# MyProjectTemplate.API

# Dependencies
Required developer/user dependencies: .NET 8 SDK/runtime, Node.js (with npm or pnpm), run dotnet restore and npm install before first run, and TODO

# Run Instructions 
1. Open Command Prompt and execute the following commands
```
TODO
```
2. Run the project template API normally
